# nixpacks.toml

[phases.setup]
nixPkgs = [
  "pkgs.python311", 
  "pkgs.opencv", 
  "pkgs.mesa", 
  "pkgs.stdenv.cc", 
  "pkgs.ffmpeg",
  "pkgs.libGL",
  "pkgs.glib"
]
nixLibs = [
  "pkgs.libGL",
  "pkgs.mesa",
  "pkgs.opencv",
  "pkgs.ffmpeg",
  "pkgs.stdenv.cc",
  "pkgs.glib"
]
